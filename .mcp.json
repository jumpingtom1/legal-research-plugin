{
  "mcpServers": {
    "courtlistener": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "/home/tom/claude/legal-research-plugin/mcp-server",
        "python",
        "server.py"
      ],
      "env": {
        "COURTLISTENER_API_TOKEN": "${COURTLISTENER_API_TOKEN}"
      }
    }
  }
}
