[project]
name = "courtlistener-mcp"
version = "0.1.0"
description = "MCP server for searching CourtListener case law"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.14.0",
    "httpx>=0.27.0",
]

[project.scripts]
courtlistener-mcp = "server:main"
